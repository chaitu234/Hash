#include<stdio.h>
int main(){
    int n,rev=0,i,rem,rem2;
    printf("Enter the number to be displayed larger:");
    scanf("%d",&n);
    while(n!=0){
	rem%=10;
	rev=(rev*10)+rem;
	n/=10;
    }
    while(rev!=0){
	rem2%=10;
	switch(rem2){
	    case 
    }
}